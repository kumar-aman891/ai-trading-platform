"""Pure domain kernel — no I/O, no framework dependencies (rules/01-architecture.md).

Populated in Phase 1 Step 4: types, money, Clock, IdGenerator, errors,
TradeProposal, the risk engine, ApprovedOrderIntent, Order/Fill/Position,
kill switches, AuditEvent, and ports. Depends only on the Python standard
library.
"""
