"""ORM models, one module per schema: core, audit, paper, live.

Empty in Phase 1 Step 2. The `live` module will define no tables in Phase 1 —
migration 0002 creates an empty, ungranted `live` schema only (§5.4 of the
approved Phase 1 plan).
"""
