# Contract tests

Deliberately empty in Phase 1 Step 2. Port-fake conformance tests and the
paper executor's payload-rejection contract land alongside the modules they
constrain (Phase 1 Steps 4 and 14).
